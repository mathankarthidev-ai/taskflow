"use client";
import { useMemo, useState } from "react";
export function ProfitCalculator(){const [revenue,setRevenue]=useState(250000),[costs,setCosts]=useState(145000);const profit=useMemo(()=>revenue-costs,[revenue,costs]);return <article className="panel calculator"><p className="eyebrow">COMPANY FINANCE</p><h2>Profit & loss calculator</h2><label>Monthly revenue<input type="number" value={revenue} onChange={e=>setRevenue(Number(e.target.value))}/></label><label>Monthly costs<input type="number" value={costs} onChange={e=>setCosts(Number(e.target.value))}/></label><div className={profit>=0?"profit":"loss"}><span>Net {profit>=0?"profit":"loss"}</span><strong>₹{Math.abs(profit).toLocaleString()}</strong><small>{revenue?`${(profit/revenue*100).toFixed(1)}% margin`:"No revenue"}</small></div></article>}
