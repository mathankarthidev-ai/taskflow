import { signIn } from "@/app/actions";
const people=["maya","oliver","priya","daniel","aria","noah","alex"];
export default async function SignIn({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="auth"><section className="auth-card"><div className="logo">✓</div><p className="eyebrow">TASKFLOW</p><h1>Work moves better together.</h1><p className="muted">Sign in to your company workspace.</p>{error&&<p className="error">{error==="domain"?"Please use your company email address.":"That email isn't in the demo workspace."}</p>}<form action={signIn}><label>Company email<input name="email" type="email" required placeholder="you@taskflow.company" /></label><button className="primary full">Continue with Google <span>→</span></button></form><p className="demo-title">Demo accounts</p><div className="demo-grid">{people.map(p=><form action={signIn} key={p}><button name="email" value={`${p}@taskflow.company`} className="demo">{p}@taskflow.company</button></form>)}</div></section></main>}
